import { OnlineFriendsFilterPipe } from './online-friends-filter.pipe';

describe('OnlineFriendsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OnlineFriendsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
